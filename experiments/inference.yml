name: inference
save_dir: saved/
seed: 1234
target_devices: [0]
verbose: 2

data_loader:
  type: CloudSegTestDataLoader
  args:
    batch_size: 32
    data_dir: data/raw/
    nworkers: 8
    pin_memory: true

output:
  h5: data/predictions/raw-predictions.h5
  log: data/predictions/log.csv
